--- libsyncml-0.4.4/libsyncml/transports/obex_client.c.orig	2007-04-03 13:50:34.988830000 -0400
+++ libsyncml-0.4.4/libsyncml/transports/obex_client.c	2007-04-03 13:51:36.470669000 -0400
@@ -389,7 +389,13 @@
 	    
 	    /* Make the transmission raw (8 bit clean, no echo etc) */
 	    /* Set the speed to a higher value (9600 would be default) */
-	    cfmakeraw(&tio);
+	    // cfmakeraw(&tio);
+	    tio.c_iflag &= ~(IGNBRK|BRKINT|PARMRK|ISTRIP|INLCR|IGNCR|ICRNL|IXON);
+	    tio.c_oflag &= ~OPOST;
+	    tio.c_lflag &= ~(ECHO|ECHONL|ICANON|ISIG|IEXTEN);
+	    tio.c_cflag &= ~(CSIZE|PARENB);
+	    tio.c_cflag |= CS8;
+
 	    cfsetispeed(&tio, B115200);
 	    cfsetospeed(&tio, B115200);
 	
